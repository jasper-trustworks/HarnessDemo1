.next/
out/
node_modules/
.claude/
.devcontainer/
src/db/migrations/
src/coverage/

# git worktrees (checked-out branches inside the repo) — formatted on their own
# branches, not with the main tree (.claude/ above already covers .claude/worktrees/)
/.worktrees/

# Devcontainer null-device mounts (character special files at workspace root)
/.bash_profile
/.bashrc
/.gitconfig
/.gitmodules
/.idea
/.mcp.json
/.profile
/.ripgreprc
/.vscode
/.zprofile
/.zshrc
